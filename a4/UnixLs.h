#include <stdio.h>
#include <stdlib.h>

static void setParams(int count, char* input[]);
static void UnixManager(char* path);
static void printDirectory(char* path);
static void printFile(char * file, char* path);

